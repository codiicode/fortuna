name = "fortuna-cron"
main = "worker.js"
compatibility_date = "2024-12-01"

[triggers]
crons = ["* * * * *"]  # Runs every minute, checks if round has expired

[vars]
SITE_URL = "https://fortunaonsol.com"
# DRAW_SECRET is set as secret via: wrangler secret put DRAW_SECRET
